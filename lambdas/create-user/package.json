{
	"name": "hackaton-create-user",
	"version": "1.0.0",
	"description": "Lambda function to create user",
	"main": "index.js",
	"scripts": {
		"build": "npm install && zip -r lambda.zip ."
	},
	"dependencies": {
		"aws-sdk": "^2.1531.0"
	}
}
